# Virtual environment and Python caches
.venv/
__pycache__/
*.py[cod]

# Jupyter scratch
.ipynb_checkpoints/

# Corpus (several GB). Not committed.
data/raw/
data/interim/
data/processed/*.jsonl
data/processed/splits.json
data/processed/clean_notes.json
data/taxonomy/

# Crawl and one-off helpers. Kept on disk, not part of this repo.
download/
start_notebook.bat
upload_models.py

# Notebook / IDE leftovers
notebooks/pyrightconfig.json
results/*_labels.json

# Trained weights (416 MB per model). Published on the Hugging Face Hub instead.
# Metrics live in results/ so they stay in version control.
models/
